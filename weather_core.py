"""Compatibility shim: every mission-core name now lives in the wxcore package.

This module is import surface only — implementations are in wxcore.models,
wxcore.geo, wxcore.feeds, wxcore.wind, and wxcore.mission. Tests and the UI
may keep importing from weather_core; new code should import from wxcore.
"""

# ruff: noqa: F401

import requests  # re-exported so tests can patch the shared HTTP module

from wxcore.models import (
    _safe_int,
    _safe_float,
    DEFAULT_TZ,
    FLIGHT_LEVELS,
    EASTBOUND_CRUISE_LEVELS,
    WESTBOUND_CRUISE_LEVELS,
    SEGMENTS,
    MAX_WIND_STATION_DISTANCE_NM,
    MULTI_REGION_ROUTE_DISTANCE_NM,
    CRUISE_BIN_DISTANCE_NM,
    GAIRMET_SNAPSHOT_HALF_WINDOW,
    GAIRMET_HORIZON_FALLBACK_LIMIT,
    TCF_VALIDITY_HALF_WINDOW,
    PIREP_ALTITUDE_HALF_BAND_FT,
    PIREP_VALID_BEFORE,
    PIREP_VALID_AFTER,
    PIREP_BBOX_PADDING_SINGLE_DEG,
    PIREP_BBOX_PADDING_MULTI_DEG,
    ALTITUDE_BAND_OVERLAP_TOLERANCE_FT,
    DEFAULT_HAZARD_TOP_FT,
    WIND_COVERAGE_PROBE_ALTITUDE_FT,
    IDW_DISTANCE_SOFTENING_NM,
    IDW_MAX_STATIONS,
    ALTERNATE_DIVERSION_FLIGHT_LEVEL,
    FUEL_BURN_GPH,
    FIXED_FUEL_GAL,
    FEET_PER_NAUTICAL_MILE,
    NOAA_API_BASE_URL,
    WINDTEMP_TOKEN_PATTERN,
    WINDTEMP_GROUP_PATTERN,
    RISK_LABEL_BY_SCORE,
    MISSION_RISK_LABEL_BY_SCORE,
    AirportData,
    FuelLedger,
    build_fuel_ledger,
    MissionPoint,
    MissionBrief,
    MissionRiskSummary,
    MissionRiskThresholds,
    TerminalRisk,
    TerminalForecastPeriod,
    AirportWeather,
    LegalAlternateAssessment,
    ForecastQualityCheck,
    AlternateRangeRing,
    WindTempPoint,
    FeedStatus,
    NoaaWeather,
    HazardArea,
    SegmentHazard,
    RouteWindModel,
    FlightLevelProfile,
    RouteVerticalProfilePoint,
    RouteVerticalProfileHazardSpan,
    RouteVerticalProfileWaypointMarker,
    RouteVerticalProfile,
    normalize_icao,
    is_westbound_route,
    preferred_baseline_flight_level,
    cruise_flight_levels_for_direction,
)

from wxcore.geo import (
    great_circle_distance_nm,
    _initial_track_deg,
    _route_point_at_distance_nm,
    _route_track_at_distance_nm,
    _polygon_from_latlon_dicts,
    _polygons_from_geojson_geometry,
    _circle_polygon_nm,
    _point_in_polygon,
    _point_in_any_polygon,
    _segments_intersect_xy,
    _route_samples_for_interval,
    _route_samples_cross_polygon_edges,
    _route_interval_intersects_polygons,
    _hazard_valid_for_time,
    _latest_gairmet_valid_to_by_source,
    hazard_applies_at,
    hazard_label,
    summarize_segment_hazard,
)

from wxcore.feeds import (
    _format_time_12h,
    _format_local_with_zulu,
    _format_iso_time_local_with_zulu,
    _format_unix_time_local_with_zulu,
    _summarize_wind,
    _parse_visibility_sm,
    _lowest_ceiling_ft,
    _clouds_confirm_unlimited_ceiling,
    _add_terminal_reason,
    _flight_category_risk,
    _visibility_risk,
    _ceiling_risk,
    _surface_wind_risk,
    _weather_string_risk,
    _llws_risk,
    _terminal_risk_from_fields,
    _terminal_risk_from_metar_row,
    _terminal_risk_from_taf_row,
    _taf_period_from_dict,
    _taf_periods_from_row,
    _summarize_clouds,
    _decode_signed_tenths,
    _summarize_metar_remarks,
    _summarize_metar_row,
    _summarize_taf_period,
    _summarize_taf_row,
    _load_airport_db,
    _load_station_alias_coords,
    _lookup_windtemp_station_coords,
    _lookup_airport_from_avwx,
    _lookup_airport_from_builtin_db,
    get_airport_data,
    infer_windtemp_region,
    _coerce_latest_rows,
    _decode_windtemp_group,
    parse_windtemp_text,
    _best_column_assignment,
    _parse_epoch_utc,
    _parse_iso_utc,
    _parse_tcf_valid_time_utc,
    _parse_altitude_feet,
    _risk_score_from_severity,
    _risk_score_from_tcf,
    _hazard_band_from_gairmet,
    _parse_hazard_areas,
    _feed_error_message,
    _status_for_count,
    _build_feed_status,
    _request_noaa_json_feed,
    _request_noaa_text_feed,
    _request_noaa_geojson_feed,
    _request_noaa_feed_job,
    _derive_noaa_confidence,
    _pirep_query_params_for_airports,
    windtemp_cycle_correction,
    select_windtemp_forecast_cycle,
    _closest_utc_day_time,
    parse_windtemp_product_times,
    fetch_noaa_weather,
)

from wxcore.wind import (
    _wind_from_to_uv,
    _interpolate_uv_by_altitude,
    _project_wind_to_track,
    _estimate_wind_components,
    build_route_wind_model,
    _sample_wind_components_from_model,
    _interpolate_temperature_by_altitude,
    _estimate_temperature_c,
    _sample_temperature_from_model,
    _standard_atmosphere_temperature_c,
    _average_route_temperature_c,
    _temperature_offset_from_model,
)

from wxcore.mission import (
    build_mission_risk_summary,
    _taf_period_overlaps_window,
    _taf_period_at_time,
    evaluate_legal_alternate_requirement,
    evaluate_terminal_forecast_quality,
    _destination_point_from_bearing_nm,
    _integrate_vertical_bands,
    _integrate_radial_vertical_phase,
    _radial_cruise_distance_nm,
    build_alternate_range_rings,
    _reference_time_at_distance_utc,
    _altitude_at_distance_nm,
    _interval_altitude_band_ft,
    _altitude_band_overlaps,
    _adaptive_route_breakpoints_nm,
    evaluate_route_hazards,
    _group_contiguous_indexes,
    build_route_vertical_profile,
    _segment_wind,
    _ias_to_tas,
    _ias_to_tas_with_temperature,
    _vertical_speed_kts,
    _along_track_ground_speed,
    _vertical_row_for_altitude,
    _phase_altitude_boundaries_ft,
    _phase_performance,
    _integrate_vertical_phase,
    _flight_level_profile,
    _flight_level_point,
    build_mission_brief,
    MissionLegPlan,
    MultiLegPlan,
    route_waypoint_airport_data,
    build_multi_leg_plan,
    MissionBriefDocument,
    build_mission_brief_document,
)
